
                INICIANDO UN NUEVO REPOSITORIO
1) se tiene un direcotrio o una carpeta donde se tiene los archivos o files que estamos trabajando
2) ejecutamos por consola, en el directorio deseado el comando git init, pasan muchas cosas
	2.1)se crea una area en memoria RAM llamada stagirng, donde de se guardan los cambios que se realizan del proyecto momentaneamente 
	2.2) se crea un repositorio el cual es llamado .git por definicion en esta se gurdan los cambios final
con git add pasa a stagirn

PARA TRAER LOS CAMBIOS DEL REPOSITORIO 
- checkout pasa los utltimos cambios del repositorio a nuestros archivos, con sus banderas podemos modificar como traer esa informacion

BREACH (ramas)
- por defecto se esta en la rama master, donde estan las versiones de los archivos, 
-  se copia la rama master y se modifica, se realiza versiones las que uno desee
- hotfix es una rama donde se realiza la reparacion de bugs en la version acutal 


la union de estas ramas se llama merge


-git es un control de vesiones , todo lo que hemos modificado anterioemente esta guardado en estas versiones
- como podemos cer visual studio code detecta que estmos utilizando un control de versiones 
- se utiliza rm --cached para borrar de la memoria 
- git diff direcicion de una version direccion de otra  versio, compara

-  git diff direccion direccion
-  git reset --hard o --solf hard es mucho mas invasivo de esta forma podemos ir a la version que deseemos 

- a git solo le importa los archivos, las carpetas las crea como rutas
- con solo git diff puede mirarse las diferencias si la version anterior esta en stagirn
- con git log --stat nos permite ver los cambios que se hicieron en cada archivo
- para ver un archivo en especifico copiamos la ruta y utilizamos git checkout ruta nombre
- para volver a la version actual se git checkout master nombre
-git log --all --graph
-git log --all --graph --decorate --oneline


                EN RESUMEN
1) se tiene un directorio de trabajo, el cual es el entorno de trabajo personal, este es la carpeta donde tenemos los proyectos
2) el COMANDO git init aparencen -una area de preparacion (Staging)
                                 -base de datos historicos del proyecto este es el repositorio local
3) con git add . hace un tracked (rastreo) este analiza el archivo y ve di hay vinculos y tambien agrega estos cambios y pasa ha la memoria ram o Staging
4) con git commit lo envia a el repositorio local


            REPOSITORIO REMOTO
-es un repositorio el cual se utiliza para trbajar en conjunto 
-para envair la version actual (la rama master), al repositorio remoto se utliza git push
-git fetch copia la ultmia version del repositorio remoto a el repositorio local, git merge une la ultma version del repositorio local a mis archivos
-git  pull copia a el repositorio local y a mis archivos 


            LAS RAMAS 
- son formas que nos permiten hacer  cambios sin afectar la rama principal
- git branch nombredelanuevarama
- con git checkout nombredelarama podemos movernos entre ramas
- una ves etando en la nueva rama todas los cobios seran modificados en la rama

        PARA HACER UN MERGE 
- merge trae todo lo que hay en el ultimo commit de la rama y los fuciona
- merge necesita enviar un mensaje, y elemina las demas versiones que se tiene 
- se recomienda hacer merge en master

        CONFLICTO 
- este ocurre cuando dos progrmadores cambiaron la misma linea de codigo
- sale un estado de stanbite mientras se elige el cambio que deseamos realizar 

- pull para importar 
- push exportar 
- git remote add nombre url,  de esta forma podemos crear
- eliminar un repositorio remoto git remote rm (nombre)
- cambiar la url del repositorio remoto git remote set-url 

        LLAVES PUBLICAS  Y LLAVES PRIVADAS
- estas dos llaves estan vinculadas matematicamente, entonces lo que se cifre con la privada tambien se hara con la publica 
- entonces se cifra con la llave publica de alquien lae vamos a enviar el menmsaje 
- la clave y contraseña esta siendo gurdada de manera local podemos pasar ese codigo a git y git hub 

- para crear una llave se utiliza el comando ssh-keygen -t (que algoritmo vamos a utilizar) rsa -b (complejidad) -C (el correo)
- primero se revisa el servidor de llaves este encendido para que este verifique y se pueda realizar la conceccio doble evall $(ssh-agent -s)

- para agregar la llave ssh-add (ruta donde esta la llave privada) 
- cada usuario y cada computadora debe tener una llave unica 

        TAG-etiqueta
-git tag -a "versio" -m "mesaje a enviar " direccion
-git show-ref --tags de esta forma podemos ver donde se agrego el tag
-los tag no son cambios y si entras en gi stastus no sale nada que enviar 
- para enviar los tags a el repostorio en git-ub se utiliza git push nombreRemoto --tags
- si se desea borrar un tag git tag -d version a borrar, esta se borra del repositorio local
- para borrar de git-ub se utiliza  git push --tags y luego git push nombreRemoto :refs/tags/version

        TRABAJAR EN CONJUNTO 
- se debe dar permiso para que ingrese este puede ser de dos formas para que ingresse nombe publico o correo publico
- el nuevo integenate no nocesesita que inicie o cree el repositorio este puede ser descargado


